import strings
import generators as generate
from strings import chars_case_rotation
from generators import repeat_items as repeat_elements

print(strings.palindrome("racecar"))
print(generate.primes_gen(46))
print(chars_case_rotation("kJoGjO"))
print(repeat_elements([0, 42, "cat", 'c']))
